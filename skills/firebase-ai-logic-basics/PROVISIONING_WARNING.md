# CRITICAL: AI Logic Provisioning

You MUST run `npx firebase-tools init ailogic` to provision the backend. 
Skipping this step will result in `PERMISSION_DENIED` errors.
Client-side tools like `flutterfire configure` DO NOT provision the backend.
